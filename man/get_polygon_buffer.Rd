% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venndir-polygon-list-label-segment.R
\name{get_polygon_buffer}
\alias{get_polygon_buffer}
\title{Get polygon_list after applying buffer width}
\usage{
get_polygon_buffer(
  polygon_list,
  buffer = -0.5,
  steps = 50,
  relative = TRUE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{polygon_list}{\code{list} with \code{"x"} and \code{"y"} elements.}

\item{buffer}{\code{numeric} value, usually negative, which
will create a buffer inside the \code{polygon_list} polygon.}

\item{steps}{\code{integer} number of steps to iterate between
\code{buffer} and \code{0}.}

\item{relative}{\code{logical} indicating whether \code{buffer} is
a fraction of the buffer width required to reduce the
\code{polygon_list} polygon to zero area.}

\item{verbose}{\code{logical} indicating whether to print verbose output.}

\item{...}{additional arguments are ignored.}
}
\value{
\code{list} polygon_list object with \code{"x"} and \code{"y"} elements.
}
\description{
Get polygon_list after applying buffer width
}
\details{
This function is intended to help apply negative
buffer width, where the intended buffer width may
result in a zero-size polygon, in which case a
progressively smaller buffer width is applied
until a polygon can be returned with non-zero area.

An added bonus, when \code{relative=TRUE} it will determine
the actual buffer value that would barely result in non-zero
area, and define that as 1x buffer, and the \code{buffer}
will be scaled to that value. So \code{buffer=-0.5} will
return an interior polygon at half the width it would take
to reduce the polygon to zero size. See examples.

Ultimately if no buffer can be applied, then the input \code{sp}
object is returned without change.

This function is useful to draw a line segment from outside
into a polygon, without drawing it to the center point of
the polygon, where a text label may already be positioned.
}
\examples{
polygon_list <- get_venn_polygon_shapes(counts=c(A=1))
plot_polygon_list(polygon_list, col="red")
polygon_list_inside <- get_polygon_buffer(polygon_list, -0.5, relative=FALSE)
plot_polygon_list(polygon_list_inside, col="cornsilk", lty="dotted", add=TRUE);

polygon_list <- get_venn_polygon_shapes(counts=c(A=1))
plot_polygon_list(polygon_list, col="red")
polygon_list_inside <- get_polygon_buffer(polygon_list, -0.2, relative=TRUE)
plot_polygon_list(polygon_list_inside, col="cornsilk", lty="dotted", add=TRUE);

# by default relative=TRUE
sp1 <- polygon_ellipses(c(3.5, 2), c(2, 3), xradius=c(2, 3), yradius=c(3, 1))
sp <- minus_polygon_list(sp1[1:2]);
opar <- par("mfrow"=c(2,2));
on.exit(par(opar));
for (i in -c(0.9, 0.75, 0.5, 0.25)) {
   plot_polygon_list(sp, col="red",
      main=paste0("buffer=", i));
   plot_polygon_list(get_polygon_buffer(sp, i, relative=TRUE),
      col="cornsilk", lty="dotted", add=TRUE);
}
par(opar);

}
\seealso{
Other venndir polygons: 
\code{\link{eulerr_to_polygon_list}()},
\code{\link{get_largest_polygon_list}()},
\code{\link{get_venn_polygon_shapes}()},
\code{\link{intersect_polygon_list}()},
\code{\link{minus_polygon_list}()},
\code{\link{nudge_polygon_list}()},
\code{\link{polygon_areas}()},
\code{\link{polygon_circles}()},
\code{\link{polygon_ellipses}()},
\code{\link{polygon_list_labelr}()},
\code{\link{polygon_list_to_xy_list}()},
\code{\link{rescale_polygon_list}()},
\code{\link{union_polygon_list}()},
\code{\link{xy_list_to_coord_list}()}
}
\concept{venndir polygons}
