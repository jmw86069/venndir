% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venndir-expand-range.R
\name{expand_range}
\alias{expand_range}
\title{Expand numeric range}
\usage{
expand_range(x, expand_fraction = 0.1, minimum_range = 0.01, ...)
}
\arguments{
\item{x}{\code{numeric} vector, or \code{list} of \code{numeric} vectors.
The input is converted to a range using \code{range(x, na.rm=TRUE)}
to ensure no \code{NA} values are included. Note that this
step will force the range to be in ascending order.}

\item{expand_fraction}{\code{numeric} value indicating the
fraction of the range defined by \code{diff(range(x, na.rm=TRUE))}
to add to the total range. When \code{expand_fraction} has
only one value, it is applied across the whole range,
which means each side is extended by half the \code{expand_fraction}.
When \code{expand_fraction} contains two values, it is
applied in order to the low, then high side of the
numeric range, and each full \code{expand_range} value is
applied.}

\item{minimum_range}{\code{numeric} value indicating the minimum
range of the output, useful when the input has zero
range, for example if \code{x=c(10, 10)}.}

\item{...}{additional arguments are ignored.}
}
\value{
\code{numeric} vector, or when input \code{x} is \code{list} the
output will also be a \code{list} of \code{numeric} vectors.
The numeric vector will contain the range after
expansion.
}
\description{
Expand numeric range
}
\details{
This function takes a \code{numeric} range (or numeric vector
and calculates its range) and expands this range by
a fraction given by \code{expand_fraction}.

When the input range is zero, the minimum absolute range
can be given by \code{minimum_range}.

The input may be a \code{list} that contains \code{numeric} vectors,
in which case the \code{list} will be iterated to produce an
expanded range for each \code{numeric} vector. Each \code{numeric}
vector is expanded independently.

This function is intended to be a simple technique to expand
x-axis and y-axis ranges of a graphical plot.
}
\examples{
x <- c(0, 10);

# expand the total range by 0.1
expand_range(x, 0.1);

# the original range is 10 units
diff(x);

# the expanded range is 11 units
diff(expand_range(x, 0.1));

# expand one side but not the other
expand_range(x, c(0.1, 0));
# this new range is 11 units
diff(expand_range(x, c(0.1, 0)))

# input with no range is extended to some minimum value
expand_range(1, minimum_range=1)
expand_range(1, minimum_range=c(1, 0))

# list input is iterated, for example xlim, ylim
xlim <- c(1, 10)
ylim <- c(1, 100)
expand_range(list(xlim=xlim, ylim=ylim))

}
\seealso{
Other venndir internal: 
\code{\link{assemble_venndir_label}()},
\code{\link{get_venn_polygon_shapes}()},
\code{\link{make_color_contrast}()},
\code{\link{make_venn_combn_df}()},
\code{\link{match_list}()},
\code{\link{print_color_df}()},
\code{\link{shrink_df}()},
\code{\link{simple_ellipse}()},
\code{\link{venndir_to_df}()}
}
\concept{venndir internal}
